on:
  workflow_dispatch:
jobs:
  exmple1:
    runs-on: ubuntu-latest
    steps:
    - run: |
        deprecated=true
        if [[ deprecated ]]; then
          echo "::warning:: Deprecated Func. was used!"
        fi  
  
